import { defineMessages } from "@formatjs/intl";

const scope = "app.components.Home";

export default defineMessages({
    title: {
        id: `${scope}.title`,
        defaultMessage: "About Us",
    }
})