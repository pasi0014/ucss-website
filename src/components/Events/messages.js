import { defineMessages } from "@formatjs/intl";

const scope = "app.components.Events";

export default defineMessages({
    eventsTitle: {
        id: `${scope}.eventsTitle`,
        defaultMessage: "Events",
    },
});