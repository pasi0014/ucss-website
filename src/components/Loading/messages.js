import { defineMessages } from "@formatjs/intl";

const scope = "app.components.Loading";

export default defineMessages({
  loading: {
    id: `${scope}.loading`,
    defaultMessage: "Loading...",
  },
});
