import { defineMessages } from "@formatjs/intl";

const scope = "app.components.NewsCard";

export default defineMessages({
  readMore: {
    id: `${scope}.readMore`,
    defaultMessage: "Read More",
  },
});
